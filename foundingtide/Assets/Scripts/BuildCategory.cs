/// <summary>
/// How the build palette groups a building (2026-09-18). One value per BuildingData, so a
/// new building appears under its heading with no UI change.
/// </summary>
public enum BuildCategory
{
    Housing,
    Production,
    Defence,
    Special
}
