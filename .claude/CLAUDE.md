# Island RTS Game - Claude Working Memory

## Critical Rules

- **Ask 2-4 targeted clarifying questions before writing code** for gameplay, UX, or bugfix changes. Polish > speed.
- Utility AI considerations are **multiplicative** (any 0.3 kills the action); momentum is **additive** after. With the 20% switch threshold, small momentum can make an action impossible to exit, and `yShift > 0` prevents early-out so momentum keeps dead actions alive. Test exit conditions and the full transition table.
- **Session log goes in `docs/PHASE_HISTORY.md`, not here.** Rules it produces go in the matching gotcha section below, one dated rule-shaped bullet. Keep this file under ~280 lines.
- **Anything a player can see gets an `Assets/Resources/Changelog.txt` entry** (`## yyyy-mm-dd — Title` + `- bullet`, newest first, player-facing). Its newest date is the main menu's "updated" line.
- **Every feature also gets a batch in `Assets/Resources/DevQuests.txt`** (2026-09-07): `## yyyy-mm-dd Feature` + `- @signal Quest` / `- Quest`, ≤ 70 chars each, newest first; add a `DevQuests.Signal("key")` at the point of effect where no signal fits. **A signal is the proof (2026-09-09): it marks the quest done AND Pass**, so a quest stays manual only when nothing in code can see it (looks, feel, "nothing bad happened over time" — no watchdogs by decision). A STATE quest ("four cutters over two trees") is a `DevQuests.IsOpen(key)`-gated check in `DevQuestTracker.Watch` (0.5 s, ActiveLists, no allocation), not a per-frame signal. The signal set in use is listed at the top of `DevQuests.txt`. That is the playtest checklist now (tracker + Esc → Information → DEV → SUBMIT REPORT → `Playtests/*.md` comes back). Delete a batch once its report is in. `docs/CONTROLS.md` keeps controls only.

## Tech Stack

Unity **6000.5.9f1** · C# · URP 17.5.0 · AI Navigation 2.0.14 · TextMeshPro + uGUI 2.5.0 · Input System 1.20.0 (legacy `Input.*` still used, `activeInputHandler: Both`) · Git.

## Project Structure

Repo root is `README.md`, `SimSweeps/` + `tools/run-sim.ps1`, and `islandrts/`. **Every other doc lives in `docs/`** (reorganized 2026-09-10): the two live plans `ALPHA_PLAN.md` and `ARCHITECTURE_LAP_PLAN.md` (the current plan), then PHASE_HISTORY, CONTROLS, SIMULATION, MENU_WIREFRAMES, SCALING_NOTES. `docs/RIVAL_COLONIES_PLAN.md` is the live working detail for lap step 3 (rival colonies: Slice A arrival + territory, Slice B the governor refactor). `docs/plans/` is the archive — built, superseded or parked design docs kept for their locked decisions (TERRAIN_SYSTEM, RESEARCH_AND_DAYS, CRAFTING_AND_PLAYER_CHARACTER built; PHASE_10_VISUAL_OVERHAUL superseded; COLONY_EXPANSION parked). Each opens with a status banner saying what is true now; the body below it is verbatim as written, so never read a status line inside one as current.

`islandrts/Assets/`: `Scripts/` (root-level systems + `AI/` `Items/` `Terrain/` `UI/` `Sim/` `Shaders/`), `Editor/` (FullSetup master menu, `LowPoly/` generator+plumber+scatter table, OpeningSequenceSetup, TerrainSetup, NewContentSetup, `Sim/SimTools`), `Prefabs/`, `Art/` (generated low-poly library), `Settings/` (URP, IslandSettings.asset, ScatterSettings.asset), `MainIsland.unity` (**the game scene**), `MainMenu.unity` (entry point), and unused stock `Scenes/SampleScene.unity` (not in the build; `Setup Everything` writes the scene list as MainMenu → MainIsland).

### Key Scripts (only where the name doesn't tell you)

| Script | Purpose |
|--------|---------|
| `DayNightCycle` | 150s day / 75s night (scene values, 2026-09-16), phase clock, lerps `LightingPreset` SOs, static `OnDayStart`/`OnNightStart`, `clockPaused` |
| `RaidDirector` | Runtime-added to `EnemySpawner` at Awake; rolls at dawn whether raiders land and how many. `EnemySpawner.SpawnRaid(count)` only lands what it is told |
| `Factions/Population` | Per-faction roster (`Colonist{unit, home}`), `IHousing` providers, arrival timer, food, `ReplaceUnit`, `HousingProviders`; `PopulationManager` is only the scene tunables + the ticker |
| `BaseBuilding` | The campfire: `AssignWorker`, `SpawnWarrior`, `SpawnColonist`, `Stockpile`, station host; assigns itself to `Faction.Campfire` in Start |
| `UnitBase<T>` | CRTP base for Worker/Warrior/Enemy: registry, `CachedHealth`/Agent, state text, stuck resolver. Jobs are `Worker.hasJob` / `SetJob` / `ClearJob` |
| `PlayerCharacter` | The named castaway: `CommandAt` right-click tasks, six-slot `Inventory`, knock-out not death |
| `BuildPlacement` | Build-mode coordinator over plain-C# helpers `GhostPlacer` / `WallLinePlacer` / `DemolishTool` / `NoBuildZoneRenderer` (not MonoBehaviours) |
| `WallConnector` | Procedural wall meshes at runtime (6 shapes + 6 gate variants); `WallGrid` is the O(1) lookup |
| `Terrain/TerrainGrid` | `[DefaultExecutionOrder(-100)]`; builds island + NavMesh in Awake. `SampleHeight` / `IsBuildable` / `IsReachable` / `FlattenArea` / `SizeScale` / `RunSeed` / `CoveCenter` / `CampfireSite`. `IslandGenerator` is the pure seeded pipeline behind it, `IslandSettings` the knobs |
| `UI/MenuBuilder` | Code-built uGUI widgets; `MenuScreens` = every screen, `MenuFlow` = scene flow |
| `WorkerAssignmentUI` | Campfire/station panel (tabs); `ResourceUI` = HUD chips + breakdowns; `UI/PlayerHUD` = bottom inventory strip |

## Architecture

### Utility AI

No state machines. Per unit: `AIBrain` holds `ActionOption[]`, each with `Consideration[]` (0-1, each with a `ResponseCurve`) and an `ActionExecutor` (OnEnter/OnUpdate/OnExit); `AIBlackboard` is the zero-GC per-unit cache.

Every 0.25–0.35s (randomized per unit) the brain scores `basePriority × Π(considerations) + momentum (if current)`. Early-out at 0.001; best must beat current by 20% to switch. Per-frame budget `Clamp(activeBrains × dt / MinEvalInterval, 5, 64)`; throttled evals are deferred, never dropped, and `ForceReeval()` is budgeted, not a bypass.

**Worker:** Gather, Return, Pickup, then the jobless ladder Build 1.0 > Craft 0.95 > Repair 0.9 > Forage 0.85 (each gated by `IsJobless` + `SpecialtyAllows`), Sleep 1.45, Idle, Flee (garrison in nearest hut), Leave. **Warrior:** Engage, Intercept, DefendWall, Patrol, Retreat, Heal. **Enemy:** one `EnemyAttack` action with an imperative priority target function (gate override → hostile warrior in range → hostile campfire-proximity commit → reachable hostile hut/tower/workshop/shipyard → hostile wall/gate at 0.3× → nearest hostile campfire).

### Patterns

- **Singletons:** AudioManager, WallGrid, AIWorldState, GameManager, CombatEffects, CameraShake, BuildingDatabase. No `DontDestroyOnLoad` (stale state across restarts) except `DebugMenu` and `SimRunner`, which hold no game state.
- **`ActiveRegistry<T>`** gives static O(1) lists for every unit, building, node, site and pickup type. `FindObjectsByType` is **banned** — use `X.ActiveList` with an index loop. Unique lookups use `FindAnyObjectByType<T>()` (`FindFirstObjectByType` is obsolete in 6000.5).
- **Targeting:** everything implements `ITargetable` (which is `IOwned`: every unit and building has a `Faction`). Scans go through `TargetingUtil.FindNearest` / `FindNearestOwned(list, …, owner)` / `FindNearestHostile(list, …, me)` — **the registries stay global and ownership is a filter on the scan**, never a second list. A warrior's "enemy" is `FindNearestHostileCombatant` (raider bodies AND hostile colonies' warriors); `HostileCentroid` is the Intercept rally. Target state through `bb.SetTarget` / `ClearTarget` / `IsTargetAlive`, carve-safe destinations and ranges through `TargetingUtil.GetApproachPoint` / `EdgeDistance`. Never hand-roll a scan or an approach point.
- **Performance:** zero GC in Update and AI eval; `AINavHelper` throttles SetDestination (20/frame) and CalculatePath (2/frame); fighter density grids per faction (`AIWorldState.GetNearbyHostileCount(pos, me)`, cell 10, sums the hostile factions' grids); dirty-checked UI text; audio preloaded; staggered per-unit timers. **A per-unit label is built only when its inputs change (2026-09-16):** `FloatingText.SetText` dirty-checks the TMP write, but the concat before it ran every frame per unit, labels hidden or not — 50 units of `"Attacking " + name` was the steady-state GC that froze the whole village for a blink. `AIBrain.BeginFrame` clamps its `deltaTime` at 1/30 so a hitch frame cannot authorise an every-brain catch-up frame. `PerfLogger` is `DontDestroyOnLoad`: read `gcAllocB` / `gcColl` / `carve` / `blocked` around an F6 MARK before guessing.
- **Point-of-effect reads:** difficulty multipliers, settings, `Knowledge` (unlocks, research, the Workshop multipliers) and stockpile capacity are read where they take effect, never pushed. Run-scoped choices (`Difficulty`, `IslandOptions`, `PlayerProfile`, `TerrainGrid.RunSeed`) are snapshotted by `MenuFlow.NewGame` and deliberately kept by `Restart`.
- **Building:** `BuildingData` SOs in `BuildingDatabase` keyed by `BuildingType`. Wall lines are click-start/click-end (L-path, or Shift for Bresenham); G converts wall→gate; Delete/X demolishes at 50% refund, campfire protected. Placement flattens a pad; walls follow terrain per cell. Construction advances only via `ConstructionSite.AddLabor` — no auto-build — from jobless colonists **or the player's character** (`PlayerCharacter.CommandBuild`, right-click a site, 2026-09-10; it takes no `RegisterBuilder` slot, those are the colonists' queue). **A colony with no jobless colonist could otherwise never build again** (no hut → no housing → no arrival → no jobless colonist); that deadlock killed all six runs of the 2026-09-10 lab sweep. `BuildingData.metalCost` (2026-09-04) is read by every four-resource cost overload (placement, refund, repair; walls keep three-arg calls). `requiresShore` = within `GhostPlacer.ShoreRadius` 6 of water (a beach-band width, NOT `SizeScale`d) via `TerrainGrid.IsNearWater`; `buildTimeOverride` exists because huts and the Workshop share one site prefab. The escape is the victory path with `GameManager.isEscape`; `EscapeInProgress` (static) blocks input during the beat.
- **Calendar:** 30-day run. `RaidDirector` rolls at dawn — `chance = (0.15 + 0.2 × quietNights) × raidFrequency`, never before day 3, forced after 5 quiet nights, **and never within `minQuietNights` 2 of the last raid unless it is the calendar's final night (2026-09-10 — back-to-back raids killed two lab colonies before a spear could be made)**. Size frozen at roll time: `round((2 + 0.4 × day + 0.08 × prosperity) × enemyCount)`. Victory = dawn after day `daysToSurvive` (Normal 30, Peaceful/Relaxed 20; Hard/Brutal raise `raidFrequency` instead of length). **A size multiplier above 1 ramps in over `RaidDirector.SizeRampDays` 7 after `firstRaidDay` (2026-09-16):** Brutal's 1.7x landed 11-15 raiders on the first eligible night and lost 18/18 overnight runs by day 7; Peaceful/Relaxed apply in full. **A night lasts until the last raider dies (2026-09-07):** `DayNightCycle` holds `currentTimeOfDay` a hair under 0.25 while any `Enemy` is alive (`DawnHeld`, capped by `maxDawnHoldSeconds`, ≤0 → 180), so `EnemySpawner`'s dawn despawn only ever fires on the cap.
- **Economy layers:** four pooled resources (`ResourceType`, Metal appended last — **never reorder**) ← items (`ItemCatalog`: materials, resources-in-hand, tools, equipment) ← research (`ResearchCatalog`, one-time, grants `Unlocks.Kind`s and hands over its tool) and repeatable recipes (`CraftingCatalog`, gated by a research id). Stations run the queue only while someone stands at the bench; costs are paid on completion.

---

## Utility AI Gotchas

- Order considerations cheapest-first, pruning against the running best (`ResourceAvailability`: type → sqr-distance cull → `distance >= bestScore` → expensive availability checks). `bb.nearestEnemy` must be populated by `EnemyPresence` **first**; zero-cost gates (`IsJobless`, `Unlocks.Has`) also go first so the action early-outs before any scan.
- `ReturnUrgency` uses `max()` of 4 signals, not multiplication; its no-node branch sends a worker home with a partial or mismatched load.
- `IsTargetAlive` must handle Unity destroyed-object null: re-fetch Health if null, don't return true.
- Momentum stops flip-flopping but also stops switches: Flee's `ThreatNearby` yShift must be 0; Heal uses zero momentum and a curve scoring exactly 0 at full HP; Build/Repair have no yShift so a finished site scores 0 despite momentum.
- **Resource nodes and buildings CARVE the NavMesh.** ANY destination on one goes ClosestPoint → `NavMesh.SamplePosition` → edge-distance, caching the collider on assignment (`bb.currentTargetCollider`). A center-based target sits inside the carve, so agents stop at the boundary and never "arrive" — same reason reachability tests must path to a sampled point, not a center (center → always `PathPartial`).
- Interaction ranges are **edge distances** (`TargetingUtil.EdgeDistance`); a center-based threshold smaller than a building's half-extent can never trip.
- `AINavHelper.TrySetDestination` returns Unity's real result — honor it, retry next frame on false. `isStopped = false` with no queued path is the "ghost moving" freeze.
- **Never `ResetPath()` just to force a new destination** — it zeroes velocity that frame; `SetDestination` swaps the path in place. `ResetPath` is for genuinely stopping.
- **Stagger every per-unit retarget/recalc timer in `OnEnter`** (`Random.Range(0f, interval)`) or a whole wave re-paths in one frame. Likewise a new `ForceReeval` broadcast on a static death/damage event is a population-scaled spike — radius-filter it, accept a frame of latency.
- Re-roll `avoidancePriority = Random.Range(30, 70)` whenever an enemy retargets from a dead target (ORCA stuck-dance otherwise).
- **A raider spawns only where a NavMesh PATH to the fire exists, and one that makes no progress for 20 s is warped toward the fire onto such ground (2026-09-10):** `EnemySpawner.CanReachFire` / `FindReachableToward` (the flood-fill `IsReachable` + a 4 m snap dropped raiders on disconnected mesh slivers, where `StuckResolver` only warps them back onto the same sliver, and the night held to the 180 s cap). `Enemy.WatchProgress` is the watchdog; `Enemy.IsFighting` exposes attack range. **That walk never crosses a wall cell (`CrossesWall`, 2026-09-10):** a sealed ring (huts in the gate openings) left no outside point with a path to the fire, so the walk stepped through the wall and the raid landed INSIDE with the militia locked out; now it answers with the first dry point on the near side and the raiders hit the wall. For the same night, tier 1 of `EnemyAttackExecutor.PickTarget` (a warrior in range) needs a complete path (`IsPathable`) or raiders stand at a wall "Moving to Warrior" all night. `RaidDirector.RaidLurking` = raiders alive and no kill / fire hit / fighting / count change for `LurkSeconds` 30: the HUD shows "Raiders lurking" + a banner naming the Offensive key, and every sim policy's `ManageStance` goes Offensive on it and Defensive at dawn.
- `ResponseCurve.Constant` discards its input — never pair it with a scanning consideration; use `ConstantScore` for a floor.
- **A scan's "nothing found" must reach the brain as a real 0 (2026-09-08):** a yShift on a scanning consideration's curve turns "no node" into a score that beats Idle's 0.01, and the executor enters with nothing to do (the "specialists stand at the fire labelled Gathering" bug). Put any floor INSIDE the consideration, applied only when it found something (`ResourceAvailability.FloorWithNode`).
- Actions sharing logic and differing only in "what target?" should be ONE action + priority function (the enemy refactor); siblings + momentum + threshold fight on every target death.
- `AIBlackboard.SetTarget` returns true only when the target **changed**, and deliberately has no side effects — each executor decides what to reset.
- **Any executor calling `StuckResolver.UpdateMoving()` must early-return when it reports a reset** — the `onStuckReset` callback nulls blackboard fields mid-call. An executor that keeps its own destination flag must also RE-ISSUE after a reset (the reset `ResetPath`s and nobody else will): Patrol ignoring it was the 2026-09-07 "warriors frozen on Patrolling" bug. Idle guard posts stay `FireClearance` 4 u off the fire's collider edge.
- **Two radii, one grid (2026-09-16, `AI/Shared/UnitSpacing` / `UnitGrid` / `FormationSlots`):** `SolidRadius` 0.22 is the hard body, used ONLY by `UnitGrid`'s 20 Hz soft push-apart (`NavMeshAgent.Move`, clamped 1.5 m/s, a stationary unit is never moved); the agent's `radius` is the AVOIDANCE radius (0.55, workers 0.4 because the gather ring seats them 0.85 m apart — raise it with the ring, not alone) and `UnitSpacing.Apply` is the ONE writer of radius + quality (Med workers, Low the rest). Right of way is deterministic: `UnitSpacing.SetMoving(agent, carrying)` (cargo 35 / empty 50 + an id band; `Worker.RollMovingAvoidance(agent, bb.carryAmount)`), a unit inside a gate corridor is set to 20 by the grid tick, stationary stays 10. **Never `Random.Range` an avoidance priority again.** `UnitGrid.Ensure()` rides the first `StuckResolver.Initialize`; `Query(pos, r, buffer)` is the 3x3 lookup for anything that needs neighbours. The stuck ladder is in `StuckResolver` (nudge 0.75 s → re-path 2 s → pass-through 4 s → abandon 8 s + rate-limited warning); `Escalations[]` on the F3 overlay, ABANDON should read 0. Formation ranks are OWNED claims (`FormationSlots.RankOf`, per colony per kind, held through Engage, released on death/destroy, cleared by `SetStance`/`SetFormation`); Patrol never claims.
- **Avoidance role follows worker state:** `Worker.SetStationaryAvoidance` (priority 10) whenever standing still, `RollMovingAvoidance` on every moving errand. A stale priority-10 mover plows through everything. Gatherers rubber-band to their anchor if nudged > 0.5.
- **Flee = garrison:** only `FleeToHutExecutor` and `SleepExecutor` may call `Worker.SetGarrisoned`, and each OnExit always restores before another executor runs.
- **The levy is the spare stock, not a role (2026-09-16, `Factions/Militia.cs`):** there is no enlist step. `Militia.Tick` (2 Hz, from `PopulationManager`) scans the alarm — the bell (`Call` / `Dismiss`, F10) or a hostile combatant within `AlarmRadius` = HomeRadius 70 of the fire (player: also `RaidLurking`) — then `Levy()`: under the alarm, one claim per weapon in the stockpile (`SpareWeapons`) goes to the next colonist (jobless first, then nearest the fire, a garrisoned sleeper last, never a leaver) through `Worker.SetLevied`, the ONLY writer of `Worker.levied` / `bb.levied` besides the Muster executor dropping a claim the fire could not honour; no alarm takes every claim back. The weapon leaves the stockpile AT the swap (`BaseBuilding.MusterLevy`, `LevyWeapon()` = the picker's choice if in stock, else the best) and returns in `StandDownLevy` (the carried weapon, so a Rearm sticks); both walks go to the fire's drop-off slot, never a hut. Muster is 1.8 (over Flee 1.2 + 0.2 momentum and Sleep 1.45, under Leave 2.0); StandDown 0.8 (under Engage and a wounded Heal, over Patrol) after `StandDownSeconds` 30 of quiet; both executors end in a body swap, so their OnExit does nothing once `done`. `Population.Colonist.mustered` + the paused job are written only by those two. A mustered levy is in `activeWarriors` but never `currentWarriors`, so `GetWarriorCount` / prosperity / the governor's army stay full-time only; `RemoveWarrior` skips them, `DismissWarrior` on one is `StandDownLevy`, a fallen one loses the weapon. **Every governor's weapon buffer is levied at every alarm** — baselines after this are not comparable to the 2026-09-16 evening runs.
- **Colonists sleep by the clock, not by "night" (2026-09-16, `SleepUrge` / `SleepExecutor` / `Factions/Persona`):** `bb.bedtime` / `bb.wakeTime` come off the roster entry's `Persona` (midnight to dawn; Night Owl +1 h bed, Early Riser −1 h wake, Hardy both, Lazy +1 h wake and `bb.standScale` 1.5 on Idle's stand timer). Sleep is 1.45 with zero momentum: above every errand's 1.15-with-momentum by the 20% bar, below GearUp 1.5 and Leave 2.0; the executor delivers a load through `ReturnToBaseExecutor.Deliver` first, then garrisons in the home hut or lies by the fire (`BedClearance` 3 off the collider edge, bearing off the entity id, `Loiter.IsClear`). A hostile in the density grid zeroes the urge so Flee wins — EXCEPT `bb.asleepInHut` (written only by SleepExecutor while hidden): a sleeper inside a hut is where Flee would put them, so they sleep through the raid instead of popping out and back in. A `Persona` is rolled in `Population.AddColonist` from `UnityEngine.Random` (it decides behaviour, so headless and visual must agree) and survives `ReplaceUnit`; `Worker.Persona` / `Warrior.Persona` cache it, the labels prefix the name. The 150/75 clock (was 100/50) plus sleep cut labor per real day: baselines before 2026-09-16 evening are not comparable.
- **Warrior stance (2026-09-07):** `Faction.Stance` is colony-wide, read live through `GuardStance.Effective(faction)`; `StanceAllows(role)` gates Intercept/DefendWall/Patrol/Follow and `GuardStance.Allows(enemy, pos, fire, faction)` is the ONE filter behind Engage (consideration AND executor scan). Follow is Defensive for any faction but the player's. Engage walks to an `Enemy.ClaimAttackSlot` bearing, not the centre; any executor walking to a slot or ring point swaps `stoppingDistance` to 0.5 and restores it (the default `attackRange − 1` parks an archer a reach short). `Formation.TrySlot(warrior, faction, …)` (Auto→Line/Wedge/Ring by stance; Loose = executor's own spread) is a RANK in `Warrior.ActiveList`, not a claim — Intercept and Follow use it, Patrol never. Offensive = Intercept advancing to `AdvanceStandoff` 14 short of the raiders + Engage at `OffensiveEngageRadius` 20; widen that radius and the wedge dissolves. Archer `KiteStep` runs before the approach, bails on a throttled `TrySetDestination`, and on hold stances is leashed to `engageAnchor`.
- **A job change owes the fire a visit (2026-09-07):** `Worker.OnJobChanged` sets `gearingUp`; GearUp (1.5, `ThreatNearby`-gated so Flee still wins) delivers via `ReturnToBaseExecutor.Deliver` and pauses 1.5 s; only `GearUpExecutor` clears the flag.
- **Pickup vs Forage:** `PickupAvailability` is for a colonist WITH a job (own resource type, 22u attract range from itself); `ForageAvailability` is the jobless half (any type, within `HomeRadius` 70 of the campfire, `NightRadius` 30 after dusk, read from `AIWorldState.Instance.isNight` — a consideration must never `FindAnyObjectByType`). Both reuse `CollectPickupExecutor`; pickups carry `claimedBy`.
- **What is in the hands is `bb.carryType`, not the job.** Availability scores 0 while carrying a different type; a job change delivers the old load first. `Worker.OnJobChanged` releases the node claim, nulls the target, `ForceReeval`s.
- Worker spacing knobs: `Worker.AgentRadius` (0.3, what actually spaces workers), `GatherStopDistance`, `ResourceNode.GatherRingRadius`, `gatherDistance` (floored at `AgentRadius + 0.25`). Per-node capacity is `ResourceNode.GetMaxWorkers()` from ring circumference and open NavMesh samples (~10 on a tree, so capacity is not what spreads workers). **Crowd = `ResourceNode.GetWorkerCount()`** (registered + claims, 2026-09-08) in the node score and `CrowdPenalty`; a worker unclaims on arrival, so claims alone hide everyone already chopping.
- **Anything walking to the fire's edge claims a drop-off slot (2026-09-08):** `BaseBuilding.ClaimDropoffSlot` / `DropoffPoint` / `ReleaseDropoffSlot`, eight bearings owned via `Worker.dropoffSlot`, same pattern as `Enemy.ClaimAttackSlot`. A `GetApproachPoint` from the worker's own position puts a whole group on one face. Return also treats 1.5 s stopped within 3 u of the edge as arrived — a queue is a legitimately stopped agent, not a stuck one, so `StuckResolver` never fixes it.
- Unreachable-node fallback: `bb.MarkNodeUnreachable` ring (15s), set by `GatherExecutor` after 0.6s of dead-end path. **Pickups have the same ring (2026-09-09):** `bb.MarkPickupUnreachable`, set by `CollectPickupExecutor` on a stuck reset, a partial path, or 0.5 s stalled beyond 1.5 u (stalled within 1.5 u collects). A stuck reset that only drops the target re-acquires the same one next tick — the forage "metre forward, metre back" stutter. Both pickup scans skip the ring and unexplored ground.
- **Nobody idles in a gateway (2026-09-13):** `AI/Shared/Loiter.IsClear(point)` rejects a `WallGrid` cell and anything within `GateClearance` 3 of a gate centre (a gate carves nothing, so `SamplePosition` cannot see it); every loiter picker (Idle stroll, Patrol's three tiers) calls it, and every STOP checks the ground under its feet too — `IdleExecutor.Stand` steps aside up to 6 m (night included, `StandHere` afterwards with no second check), Patrol re-picks a post up to 3 times in a row. Patrol anchors on walls only, never gates. A unit walking THROUGH a gate on an errand is never touched.
- **Idle walks home, then strolls by day (2026-09-08):** far from home → the home provider's approach point, stopping 3.5u out (that is what walks a fresh arrival in from the cove); then stand 6–15 s / walk to a spot just outside a random building's `noBuildRadius` (campfire, huts, Workshop, Watchtower, Shipyard; `FireClearance` 4 off the fire's edge, 4–30 u away, 20 s walk cap). Night = stand at home. Display only — `Worker.IsIdle` and the 0.1 floor are unchanged, so any work still outscores it.

### Factions (lap step 1, landed 2026-09-09 — `Scripts/Factions/`)

- **Ownership is data.** `Faction` is a plain class: `Id`, `Name`, `Color`, `Kind` (Player / Rival / Raiders), `Campfire`, `Resources` (`ResourcePool`), `Population`, `Knowledge` (unlocks + research done + the Workshop multipliers), `Priorities` (`LaborPriorities`), `Stance` / `SetStance`, `FormationKind` / `SetFormation`. `Factions.Player` / `Raiders` / `All` / `ById` / `Register` is the registry; `Relations.Get/Set` the symmetric `Attitude` matrix (self Allied, Raiders Hostile to all, both immutable; two colonies start Neutral). **The registry is scene-keyed** (`EnsureForScene` compares the active `SceneHandle` on every access), not `sceneLoaded`-keyed, because `ResourceManager.Awake` fills the player's pool. Never cache a `Faction` in a static across scene loads.
- **BANNED (each replaced 2026-09-09):** `ResourceManager.Instance` → `faction.Resources`; `PopulationManager.Instance` → `faction.Population` (`PopulationManager` is the scene tunables + the ticker of every non-raider faction); `Unlocks.Has/Grant`, `ResearchDef.done`, `ResearchCatalog.Complete/IsDone/IsAvailable`, `CraftedUpgrades`, static `LaborPriorities`, `GuardStance.Active/Set`, `Formation.Active/Set` → `faction.Knowledge` / `Priorities` / `Stance` / `FormationKind` (`Unlocks` and `ResearchCatalog` are pure definitions; a research entry's `apply` takes the `Knowledge`); `BaseBuilding.FindAlive()` and `BaseBuilding.ActiveList[0]` → `faction.Campfire` (the campfire sets it in its own Start, clears it on death and destroy); `Instantiate` of an owned prefab → `Spawn.Owned(prefab, pos, rot, faction)`. **Any static that holds colony state is a leak across the sim's scene reloads** — `[RuntimeInitializeOnLoadMethod]` resets once per process (the contaminated baseline of 2026-09-09).
- **Who reads what:** the AI reads `bb.faction.X` (set from the unit's `Faction` in Start); UI, placement, demolish, debug and the sim read `Factions.Player.X`; a building reads its own `Faction`; `CraftStation.Faction` is its host building's. Hunger events are INSTANCE events on the player's `Population`. **Faction is read in `Start` or later, never `Awake`** (`Instantiate` runs Awake before `Spawn.Owned` sets the owner); the `owner ?? default` fallback (Player, Raiders for `Enemy`) exists for scene-placed objects only. `SimBuilder` has its own `Spawn` method, so it calls `global::Spawn.Owned`.
- **`ITargetable : IOwned`, and the registries stay global — ownership is a filter on the scan:** `TargetingUtil.FindNearestOwned` / `FindNearestHostile` / `FindNearestHostileCombatant` (raider bodies AND hostile colonies' warriors) / `HostileCentroid`. The raider tiers, Engage, `StanceTargetAvailable`, `EnemyPresence`, Intercept, Flee, Repair, Construction and StationWork all filter by relation or owner; `AIWorldState` keeps one density grid per faction (`GetNearbyHostileCount(pos, me)`) and tags walls under attack with their owner. `bb.currentTargetFaction` is cached by `SetTarget` and both `AttemptAttack`s refuse a target that is no longer hostile. Attack slots exist only on a raider body; a rival warrior is approached at its edge.
- **Rivals cheat and stay dark:** the fog gates (`ResourceAvailability`, `PickupAvailability`, `ForageAvailability`, `GuardStance.Allows`) apply to `bb.faction.IsPlayer` only; `VisionSource.Attach` is player-only and every non-player unit/building gets `FogVisibility(Visible)` instead; the minimap draws a rival's things in its faction colour on watched ground only; the combat box counts the player's warriors; `OnMouseDown` on a rival fire or colonist opens nothing; a rival campfire's death is not a defeat; `SetStance` / `SetFormation` / research / hunger signals fire for the player only. Follow reads as Defensive for any faction but the player's.
- **F4 → Rival camp** (`DebugMenu.SpawnRivalRoutine`): a second campfire 40–70 u × `SizeScale` from the player's on reachable clear ground, a hut, 100W 50F, the three gathering jobs + Militia + Construction known, four survivors, one spear → one warrior, wood/food/stone jobs; Hostile / Neutral / Allied buttons drive `Relations.Set`. F3 shows a line per colony. Known gaps closed since: prosperity counts only the player's buildings (2026-09-11) and rival arrivals land at their own cove (slice A). Still open: `UnitHoleMask` does not skip a fog-hidden rival.

### Rival colonies (lap step 3 slice A, built 2026-09-11 — `Scripts/Factions/`)

- **A colony owns its own beach.** `Faction.Cove` / `SetCove`, read by `Population.SpawnArrival` — before it every faction's arrivals landed at `TerrainGrid.CoveCenter`, the PLAYER's cove, and walked the width of the island home. `RivalFounder` sets it; the player never does, so the island's anchored cove stays the fallback.
- **`RivalFounder.Found(rival, site, cove)` is the ONE founding sequence** (fire → `yield` → hut → `yield` → survivors → `yield` → arm and employ). **The founder leaves ONE survivor jobless and searches four laps (7-19 m) for the hut (2026-09-16):** a beach fire has half of its 7-11 m ring in the sea, and the 13 camps of the overnight rivals sweep whose hut never seated sat at three people and no hut ALL RUN — warrior + two workers filled the fire's beds, nobody jobless, so nothing ever got built (a rival has no castaway). **The hut retries for `HutPlaceTries` 30 frames and warns if it never lands (2026-09-16):** `FlattenArea` re-bakes the NavMesh async, so a `SamplePosition` one frame after the fire pad fails on timing — the same island seated an Eco rival's hut and skipped a Turtle rival's, which then sat at the fire's three beds for fifteen days. `DebugMenu`'s F4 and `RivalLandingDirector` both call it, and `RivalFounder.IsClearForBuilding` is the single owner of the camp-clearance check. A debug-only founding path drifts from the shipped one inside a session.
- **`RivalLandingDirector` rides the GameManager's GameObject**, runtime-added in its `Awake` exactly as `RaidDirector` rides the `EnemySpawner` — public fields are the LIVE values, never put it in a scene. Arrival = `max(3, round(daysToSurvive / 3))` **recomputed every dawn**, because `GameManager.Start` writes `daysToSurvive` and Start order between the two is undefined; the day comes off `DayNightCycle.GetCurrentDay()`, not `GameManager.currentDay`, because this handler subscribes in `OnEnable` and can run first. It disables itself in `Start` when the run asked for no rivals, which is the default.
- **`TerrainGrid.FindShoreSite(awayFrom, minDistance, out cove, out campfireSite)`** marches 72 bearings out from the map centre to the first waterline and the FARTHEST valid one wins — deterministic on purpose, because `UnityEngine.Random`'s stream is the harness's seed and the AI's stagger. `minDistance` is `RivalLandingDirector.MinCoveSeparation` 55 × `SizeScale`; a Small island seating nobody is the right answer and gets ONE warning, not a crash.
- **Territory is a preference, not a claim** (`AI/Shared/Territory.cs`): a node or pickup outside my colony's `HomeRadius` (= `ForageAvailability.HomeRadius` 70, from MY campfire) scores as if `OutsideHomePenalty` 60 m further away. It applies to EVERY colony including the player's. Added AFTER `ResourceAvailability`'s `if (distance >= bestScore) continue;` prune — the penalty is non-negative so plain distance stays a valid lower bound, and adding it before the prune breaks the prune. `PickupAvailability` went squared → linear distance for the same reason. `ForageAvailability` needed NO change: its own-campfire radius already is the rule as a hard reject.
- Rivals stay dark: first contact is polled at 2 Hz by the director (`FogOfWar` has no per-object notion of who is looking) and fires `OnRivalMet`; the banner names no position and there is no minimap marker, by decision.
- `days.csv` gained `rival_arrival_day, rival_contact, rival_opinion, rival_warriors` at the END of the header. Column order is load-bearing for every existing reader.
- **A rival thinks with the sim's brain (slice B1 + B3, 2026-09-16 — `Scripts/Factions/Governor/`):** `GovernorPolicy` (Turtle / Rush / Eco, the old `SimPolicy`) and `FactionBuilder` (the old `SimBuilder`) are per-faction INSTANCES bound by `Governor`; they ship in every build and **nothing in them may read `Factions.Player`** — it is `faction`, and every registry count is `CountOwned` / an owner filter. `GovernorRunner` rides the GameManager's GameObject (runtime-added, fields live) and adopts any `Kind.Rival` with a campfire at 2 Hz — F4's camp too — ticking each at 1 Hz on a `Random.Range(0, 1)` offset; the personality is `SimHooks.RivalStrategy` under the sim, else `CreateRandom()` off the seeded stream (a run with no rivals never draws). The sim's player is a `Governor` ticked by `SimRunner`. What a rival needed that the player never did: `CraftStation.IsQueuedAnywhere(d, faction)` de-duplicates research per COLONY; `StationWorkAvailable`'s Crafting gate is player-only (a rival has no castaway, its jobless colonists work its bench from day one); `RivalFounder.FoundingResearch` marks its head start as research DONE, not bare flags, or the governor re-buys Woodcutting; `ColonyState.RaidLurking` is true for a rival only with a raider inside its own home radius; Eco's escape is `faction.IsPlayer` only (a rival `SetSail` would end the PLAYER's run). B2 (placement through `FactionBuilder`) and B4 (scored build order) are still open.
- **Neighbours act (slice B5 minus trade, 2026-09-16):** `Diplomacy` is a static like `Relations` (cleared by `Factions.ResetAll`) holding ONE opinion per pair; `GovernorRunner.HandleDayStart` runs `Expedition.EndAll()` THEN `Diplomacy.DawnTick()` (trespass drains, allied gain, drift, threshold flips with `DwellDays` 2 through `Relations.Set` + `OnAttitudeChanged`). Every unit hit site calls `Diplomacy.NoteAttack(bb.faction, bb.currentTargetFaction)` beside `TakeDamage` (rate-limited per pair, no-op for the Raiders). The player sees a WORD (`Diplomacy.Word`), F3 the number; `IsKnown` = first contact or F4, and the DIPLOMACY screen / Neighbours bar entry list known colonies only. **The F4 relation buttons go through `Diplomacy.ForceAttitude`, never `Relations.Set`, or the next dawn flips it back.** **Trespass is THEIR side of the shared ground (2026-09-16): inside their `HomeRadius` AND nearer their fire than mine (`Diplomacy.Trespassing`)** — two 70 m radii overlap on every island this game makes (Medium ~150 m, `MinCoveSeparation` 55), so the radius alone drained every pair 7 a dawn and every rival was Hostile by day 18 in every sim run. **An expedition is the colony's REAL warriors** (`Expedition.Send` warps them to the target's cove and `Warrior.SetExpeditionFire` swaps only the BLACKBOARD's fire, so `ThreatensColony` / Intercept / Patrol fight around the other fire; `Warrior.baseBuilding` stays home for `NotifyWarriorKilled`); a warrior cannot hit a building, so a landing is a fight with the militia and nothing more, and the trespass scan skips `OnExpedition`. `GovernorPolicy.ConsiderExpeditions` runs after `ManageStance`: relief = Allied fire with a raider in its home radius, mine quiet, half of ≥ 4; landing = Hostile, by day, surplus over `lastWantedWarriors` ≥ 3, every 3 days. **A landing party besieges (2026-09-16, the conquest test):** `Siege.TargetOf(warrior)` names the colony a non-relief party landed on, and both the Engage scan and `StanceTargetAvailable` fall through to `Siege.FindNearestBuilding` (raider order: huts/works with a complete path → wall, gate 0.3× → fire) when no fighter is in reach; a building target skips `GuardStance.Allows` for as long as the siege lasts. **Every warrior and raider blow writes `Health.LastHitBy`** before `TakeDamage`; `BaseBuilding.OnCampfireDestroyed` calls `Loot.Drop` only when a non-player fire's last hit was the player's — the pool plus stick/chunk stacks become plain resource pickups (`PickupSpawner.DropLoot`, 15W / 12F / 9S / 9M each, `MaxPickups` 40, radius 9), nothing transfers, a raider-burned fire drops nothing. `ConquerorPolicy` ("conqueror" in `Create`, never in `Names` so a rival never draws it; player-only because `DeclareWar` is) declares war at `WarDay` 12 on the first landed rival and sails every quiet day with everything past half the next raid (`MinParty` 4). `runs.csv` ends `player_landings, loot_wood, loot_food, loot_stone, loot_metal`; `rival_fate` gains `conquered`. **Night raids pick a shore:** `RaidDirector.Target` is a prosperity-weighted draw over every colony with a campfire (`Prosperity(Faction)`), `EnemySpawner.SpawnRaid(count, index, target)` aims the ring at the target's cove and publishes `TargetFire` for `CanReachFire`; `Enemy.WatchProgress` walks toward the nearest HOSTILE fire; `ColonyState.RaidTonight` / `NextRaidSize` are per target, so a policy does not arm for a raid on its neighbour. `days.csv`: `raid` now means a raid at the PLAYER's shore; `raid_at_rival, rival_opinion_pts, rival_landings, rival_relief` appended. `runs.csv` ends `rival_opinion_min, rival_opinion_max` (2026-09-16 afternoon: the hour sweep's 31 landed rivals peaked at median −8, never −40, and NEVER above 0 — nothing raises opinion but being Allied). The dawn-cap warning prints `Enemy.DescribeAlive()`; a raider with `path PathComplete`, `vel 0.0`, `rem -` and a target is `IssueMove` retrying a rejected `TrySetDestination` while `WatchProgress` leaves it "on good ground" — the dead-night shape. `RivalLandingDirector`'s warning carries `TerrainGrid.LastShoreSearch` and which check failed (all 8 overnight cases were the SECOND rival).

### Bookkeeping Gotchas (single owner)

- Worker removal has ONE owner: `Worker.OnDestroy → BaseBuilding.NotifyWorkerRemoved → Population.RemoveColonist`; roster membership is the idempotence guard. Job counts are **computed** from the roster — never add a counter back.
- Housing has ONE owner: buildings implement `IHousing`, `RegisterHousing` in Start, flag-guarded `UnregisterHousing` from both death and `OnDestroy` (demolish counts). `Population` never rescans the scene.
- Arrivals and food REQUIRE the `PopulationManager` ticker — `BaseBuilding.Awake` / `Hut.Awake` call `PopulationManager.EnsureExists()`.
- **`ReplaceUnit` before `Destroy`** when converting worker ↔ warrior, so the old body's OnDestroy finds nothing to double-count. Warriors occupy housing; dismiss returns the weapon, death loses it.
- **The player is not a colonist:** never in the roster, no housing, no job, no AIBrain, never scanned by enemies, **does not eat**. The first colonist arrives from the housing timer (~20s after the fire).
- **Food (2026-09-04):** each `Population` eats `roster × foodPerColonistPerDay × Difficulty.FoodConsumptionMultiplier` per `DayNightCycle.CycleSeconds` as a fractional debt paid one `SpendFood(1)` at a time; `Hunger` is DERIVED from `starvedSeconds` (never stored), Hungry at 0.25 day (labor ×0.6 via `Population.LaborMultiplier` in the gather and construction ticks, no arrivals), Starving at 1 day (one departure per day: jobless → worker → warrior via `DismissWarrior`). `Worker.Leave()` → the Leave action (2.0, zero-cost gate) → `Destroy` at the cove = the normal removal path. The scene manager predates the fields: `<= 0 → default`, so the sim's off switch is `foodDisabled`, not 0.
- `Health.Die` with `destroyOnDeath = false` is the player's knock-out; `Health.Heal` refuses the dead, so the revive writes `currentHealth` directly. Player health is set up in `Awake` (the prefab's `HealthBar.Start` looks it up).
- Repair pricing: 25% of build cost per full repair, charged one whole unit at a time from fractional debt, committed only when `SpendResources` succeeds. `RepairAvailable` returns 0 when the next unit is unaffordable. **A gate has no `BuildingData` — price it as its wall (`RepairCosts.TypeOf(Gate)`, like the demolish refund, 2026-09-10);** `GetBuildingData` on a gate type logs an error per call, and a jobless colonist's Repair scan calls it every eval.

---

## Runtime uGUI Gotchas (menus, HUD, panels — anything built in code)

- A `VerticalLayoutGroup` with `childControlHeight = false` IGNORES `LayoutElement.preferredHeight`. If children size via `LayoutElement`, the group must control that axis.
- A control's `targetGraphic` needs `raycastTarget = true` or it is silently inert (`MenuBuilder.SimpleImage(..., raycast: true)` for any click surface, including invisible ones: scroll viewport, slider background, toggle box, drag handles).
- **Panel heights are computed** (`MenuBuilder.FitPanelHeight`) and must force a FULL recursive `LayoutRebuilder.ForceRebuildLayoutImmediate` — nested groups report zero before their own layout pass. Restoring a `ScrollRect` position needs `Canvas.ForceUpdateCanvases()` + a forced rebuild first.
- `[RuntimeInitializeOnLoadMethod]` fires ONCE per launch, so a system that must exist in every scene (`PauseController`) also subscribes to `sceneLoaded`. `sceneLoaded` runs after every `Awake` and before every `Start` — check which one a component consumes a value in before "configuring" it there.
- `GameSettings.Apply()` runs every frame of a slider drag — change-guard anything expensive.
- **`KeyBindings.cs` is the single source of truth for every gameplay key.** No `KeyCode` fields or literals anywhere else. Escape, mouse buttons and F3/F4/F6/F7 are reserved. Any modal wanting Escape must be checked by `PauseController` (`[DefaultExecutionOrder(-50)]`) before its own handler, like `MenuScreens.IsCapturingKey`.
- **Difficulty is a run snapshot** (`Difficulty.BeginRun()` in `MenuFlow.NewGame`, before the scene loads because `ResourceManager.Awake` reads it). `Difficulty.Active` returns Normal under `SimHooks.Simulating`.
- `MenuBuilder.RowDescription` is a sibling of its row, fixed 20px, one line (TMP cannot report wrapped height before a layout pass). **Inside a `ScrollColumn` a label MAY wrap with no fixed height** (the changelog bullets): the `ContentSizeFitter` re-measures each pass and the scroll region's fixed height keeps the panel deterministic. Hanging indent is `<indent>` rich text, not a nested layout.
- `MenuBuilder.Label` WRAPS by default — single-line captions set `TextWrappingModes.NoWrap` and a fixed `LayoutElement.minWidth`.
- Scroll viewports clip with `RectMask2D`, never a stencil `Mask` (a hidden Mask's alpha-0.001 graphic quantises to 0 and culls everything). The viewport still needs a `Color.clear` raycastable Image.
- Wheel over a scrolling list belongs to the list: `CameraController.PointerOverScrollView` does an `EventSystem.RaycastAll` and blocks zoom only when a hit has a `ScrollRect` above it. Never use `IsPointerOverGameObject` — the HUD makes it true almost everywhere.
- Every scrolling list gets a permanent slim scrollbar with an **opaque white** handle Image — a `ColorBlock` MULTIPLIES the graphic colour, the same reason a transparent-at-rest button needs an opaque white Image with the alpha in `normalColor`.
- Settings affecting things created at spawn must be read per frame at the effect site (`FloatingText.LateUpdate` state labels, `HealthBar.RefreshVisibility`) or they cannot be toggled mid-run.
- Victory/defeat are `MenuScreens.Screen.GameOver`, the name popup is `Screen.NameEntry`; `MenuScreens.Back()` early-returns on both (dismissing would strand the player). `GameManager.ContinuePlaying` clears `isGameOver` BEFORE closing the menu. Anything building UI on game over needs a `SimHooks.Simulating` guard.
- `PauseController.BlockGameplayInput` is true while any menu screen is open — gameplay clicks (`GameStartController`, `BaseBuilding.OnMouseDown`, HUD chips) must check it. A gameplay panel is not a menu: `WorkerAssignmentUI.IsOpen` is consulted by `PauseController.ModeActive` so Esc closes it instead of pausing.
- Draggable UI = `DraggablePanel.Attach(handle, panel, prefsKey, default)`: panel anchored + pivoted bottom-left, handle gets a raycastable `DragSurface`. The clamp reads a 0×0 canvas rect on the build frame, so call `Clamp()` again on open.
- The campfire/station panel's tabs are nested zero-padding `Column`s; `SwitchTab` refits and re-clamps. `OpenStation(station)` hides the Colonists/Stockpile tabs. The Queue tab is a pool of 8 rows reassigned on `CraftStation.Version`.
- HUD chip breakdown rows come from `ItemCatalog` (`hudListed` + `hudCategory`), so a new item appears under its category with no UI change. The panel is parented to the ENTRY so it follows its chip. Activity strings are composed only on change; slots repaint only on `Inventory.OnChanged`.
- Bottom-of-screen overlays must clear `PlayerHUD` (sort 45, strip height): `IntroHintCanvas` is sort 70 at y 150. The raid banner sits BELOW the resource bar (~900px wide from the left).
- **Right-click never opens a panel.** Right-click on the fire deposits and works the queue; left-click on its collider opens the panel via `BaseBuilding.OnMouseDown` (left button only). Keep the gestures apart.
- **uGUI stops nothing (2026-09-09):** a ground raycast, an `OnMouseDown` and the edge pan all fire through a HUD box. A click surface that must swallow gameplay clicks publishes a static like `Minimap.PointerOver` (rect test against `Input.mousePosition`, not the EventSystem) and every gameplay click site checks it — the placement/demolish confirms, `PlayerCharacter.HandleCommandClick`, the four `OnMouseDown`s and `CameraController`'s edge pan. The minimap is one texture (`Scale` texels per fog cell) rebuilt at 8 Hz from the ActiveLists; markers are stamped pixels, never objects, and raiders draw only where `FogOfWar.IsVisible` — the landing pulse (`EnemySpawner.OnRaidLanded`) is the one thing on it that ignores the fog, by decision.
- **Text-asset screens (Changelog, Information) never type a catalog number (2026-09-07).** Prose in `Resources/*.txt`; `@name` lines in `Information.txt` ask `MenuScreens.RenderInfoTable` for a live table (`BuildingDatabase.Instance` is null on the main menu — say so, don't crash). A screen with sub-tabs must bank the old tab's scroll itself and null `activeScroll` before `Rebuild()`, which banks under the SCREEN key. Six `TabRow` captions across `OptionsWidth` wrap at `ButtonSize` — drop them to `SmallSize + 2`, `NoWrap`.

---

## Visual / Art Gotchas

- Match the menu mockup's palette, water and silhouettes — not its DOF or composition. **No DOF in gameplay.** Test lighting at the real RTS camera angle.
- **Ortho camera has a NEGATIVE near clip, COMPUTED per frame (2026-09-08):** `CameraController.UpdateClipPlanes` fits it to the bottom edge at the cloud altitude (`sceneCeiling` without clouds), 8 m steps, floored at `nearClip` −100. URP splits the cascades over [near, shadowDistance], so a deep fixed near starves the ground of shadow texels. Both near and shadow distance are QUANTISED (8 m / 10 m + hysteresis in `GraphicsQuality`) — a per-frame re-fit shifts the texel grid, which is the shadow crawl. Depth-sampling screen effects (SSAO is OFF in `PC_Renderer.asset`, SSR, depth fog, depth outlines) are still garbage at the bottom of the screen; fog would be Linear (~30/80).
- **`GraphicsQuality` is the ONLY writer of the URP asset and the sun's shadow mode** (resolution, cascades, MSAA, render scale, shadow distance; `Light.shadows` + `softShadowQuality` per light — `supportsSoftShadows` is internal); it restores the on-disk values on `Application.quitting`. Settings are `GameSettings.Graphics` presets over rows; a row change calls `DetectGraphicsPreset`.
- **Cloud shade is the sun's light cookie in LIGHT space** (`CloudCookie.shader` blits the same field `CloudPuff.shader` draws, via `CloudCommon.hlsl`; the light's transform position is the cookie origin, so the sun stays near the island centre). `CloudSystem` is runtime-added by `DayNightCycle` (public fields live, never in the scene); its lighting multipliers are statics that read 1 when absent. Materials must stay in `Resources/Clouds` or a build strips the shaders.
- `LightingPreset` SOs are the runtime source of truth for sun + ambient; `DayNightCycle.Start` forces `AmbientMode.Trilight`. A new serialized field must be written into **both** .asset files (a missing YAML key is 0, not the C# default). Night is a fixed moon pose (`moonElevation` / `moonYaw`) Slerp-blended through dawn/dusk.
- Bloom threshold stays 1.0; hero assets glow via HDR emission (~3). Hover feedback is **emission** (`HoverGlow` writes `_EmissionColor` + `_EMISSION`, glow = the slot's own colour pushed toward gold), never `material.color`, so it survives the `OccluderCutout` shader swap (same property names). Pickups glow only on hover (idle 0 / hover 3.1 @ blend 0.85); nodes hover 1.7 @ 0.4. Node prefabs serialize `hoverGlow` — a code default alone does not reach them.
- **`renderer.material` is slot 0 only; reading `.materials` INSTANTIATES.** Multi-submesh art needs `RendererTint.Collect` once in Start, then `SetColor` / `RestoreColors`. **One collector per object:** `ResourceNode.EnsureNodeMaterials()` is the single collector for a node, and `HoverGlow.Bind` / `OccluderCutout` take those same instances. Never read `.materials` in Update.
- **Occluders open a WINDOW, they do not fade (2026-09-08).** `OccluderCutout` is runtime-added by `ResourceNode.Start` (every node type), `Hut` / `Watchtower` / `Workshop` / `Shipyard` / `Wall` / `Gate` `Start`; on its first LateUpdate it swaps the object's material INSTANCES (via `IMaterialSet`, else its own collect) from URP Lit onto `Resources/Shaders/OccluderCutout.shader` — a copy of Lit whose ForwardLit fragment samples the global `_UnitHoleMask` and `clip`s where the fragment is nearer than the unit (dithered rim). `UnitHoleMask` (runtime-added, fields live) blits one soft disc per unit (R coverage, G view depth of the FARTHEST covering unit) into a quarter-res RGHalf RT every LateUpdate, `MaxUnits` 128, player first. No per-object silhouette test, no height gate, no transparency toggle; ShadowCaster and depth passes are stock so a window never shows in a shadow. Both shaders live in `Resources/Shaders` or a build strips them; the mask is a GLOBAL texture on purpose (a material property would shadow it).
- **Fog of war rides the cutout shader (2026-09-09).** `FogOfWar` (runtime-added by `TerrainGrid.Awake`, 2 m cells, `IsExplored` / `IsVisible` read the RAW grid) uploads the global `_FogMask`; `FogOfWar.hlsl` is applied after `LitPassFragment` in `OccluderCutout.hlsl` and in the water shader. Unit windows are the `_UNIT_CUTOUT` keyword (`multi_compile`, NOT `shader_feature`: every material on the shader is runtime-made, so a build would strip the ON variant); `FogMaterials.For` gives terrain bands and scatter decor a keyword-off instance — a stock-Lit surface is unfogged. Sight = `VisionSource.Attach` in Start with the consts on `VisionSource` (OnEnable/OnDisable registration so a garrisoned worker stops seeing). Hiding = `FogVisibility` (raiders `Visible`, nodes/pickups `Explored`): renderers off + root to Ignore Raycast, colliders stay live for approach points; `UnitHoleMask` skips a hidden raider. **A show/hide toggle restores only what it found ON (2026-09-09):** `Tree.prefab` still carries the old FBX tree with its renderers disabled, and `FogVisibility` re-enabling "every renderer" drew both trees at once. Raider AI never reads the fog. `_FogParams.w` 0 = no fog (menu, showcase). **Any map-sized mask sampled by the water must read a fixed value past the map edge, never Clamp's edge texel** (depth map `BorderDeepTexels` ring; fog mask uv-range test + texture-only `edgeFade` over `EdgeFadeCells` 6, 2026-09-09) — the water plane runs to the horizon, so a Clamp smears the last column across the ocean. A depth RAMP in from the border was tried and cut light water under land: the real fix is `TerrainGrid.OceanMargin` (below). `revealAll` is a live flag, never written into the grid. **Unexplored REPLACES the lit colour with `_FogColor` (`FogOfWar.unexploredColor`, linear), never scales it (2026-09-12):** a 4% multiply left noon sunlight and slope shading readable, so the island's relief showed through by day while its hidden trees did not.
- **The fog gates are three reads at sites that already decide (2026-09-09):** `ResourceAvailability` skips a node on unexplored ground after the distance cull (scan time only — a worker AT a node is on explored ground); `GuardStance.Allows` refuses a raider on ground nothing is watching (`IsVisible`), and because that is the ONE filter behind Engage it covers the consideration and both executor scans (a raider in reach is inside its warrior's own sight, so an engaged target is never dropped by it); `GhostPlacer` and `WallLinePlacer.CellBlocked` refuse unexplored cells, mirrored in `FactionBuilder` (player-only there). Pickups had the same gate a session earlier. Intercept, DefendWall, worker Flee and the raid banner deliberately still read the raw registries. The sim stamps the grid too, so sweeps play under the fog.
- **`TreeVariance` swaps mesh + materials in `Awake`, never `Start` (2026-09-08).** `ResourceNode.Start` collects instanced materials; when the variance ran after it, glow and fade wrote to copies nothing drew — the "some trees never fade" bug. Anything that replaces `sharedMaterials` on plumbed art must run before the collectors.
- **`IMaterialSet` is how a second system finds an object's existing material instances** (2026-09-08): `ResourceNode`, `Workshop` and `Shipyard` implement it, `OccluderCutout` asks for it and only collects its own when nobody answers. Reading `.materials` twice on one object is the "hover glow and fade fight" bug. `OccluderCutout` swaps only materials whose shader is URP Lit, so a health-bar sprite under the same root is left alone.
- **Art is base-pivot at scale 1, mounted on a `Model` child; root scale stays 1.** Never assign an art mesh to a squashed root MeshFilter. Resetting a root's scale un-squashes `HealthBar` / `FloatingText` children — retune offsets. `NavMeshAgent.baseOffset` 0 for base-pivot art; agent radius/height are world-space, leave them.
- **A placement ghost, the player, and resource nodes must never be visible to the Default-layer ground raycast.** Ghost colliders are disabled + layer Ignore Raycast; node click boxes are layer 8 `Nodes`; pickup click boxes are layer 7 `Pickups` (added in `Awake`); the player prefab has no click collider. Ghost prefabs take the art MESH on their root renderer with one `Mat_Ghostbuilding` per submesh (no nested art prefab). Ghosts snap, they do not lerp.
- Walls cannot be plumbed by prefab swap (`WallConnector` overwrites the root MeshFilter at runtime) — re-material only. `LowPolyAssetGenerator` writes `Assets/Art/`; `LowPolyPlumber` mounts it on gameplay prefabs from a table — **an art category not in the plumber's table is invisible in-game.** `Tree.prefab` variants are art PREFABS (`TreeVariance` copies mesh + materials). A new unit category needs the enum value, the `Add…Impl` partial, a menu item and the showcase row.
- `GridOverlay` (F2, auto-shows in build mode, never G) draws only buildable cells as ONE `MeshTopology.Lines` mesh draped on the terrain, boundaries on half-offsets.
- `CombatEffects.FadeOutUnit` needs `GetComponentInChildren` (art lives on the Model child). `HudTimeDial` replaced `DayNightCycle.OnGUI`; day and night each map to their own half of the 0..1 parameter at their own rate.
- **Resource node scaling:** trunk-tight carve radii 0.45 / 0.5, `GatherRingRadius = obstacle.radius + 0.55`. Depletion shrink and gather wobble scale the **Model child, never the root** (root scale drives the obstacle → re-carve every tick). `SetupNavMeshObstacle()` overwrites serialized obstacle values; root transform scale still scales them.

### Terrain / Water Gotchas

- `TerrainGrid` does EVERYTHING in `Awake` (generate → chunks → water → deep-water NotWalkable volume below −0.4 → `BuildNavMesh()`), so every `Start()` finds a finished world. The surface is `CollectObjects.Children` + `PhysicsColliders` — only chunk colliders and the modifier volume feed the bake, so water and scatter must never be children of `Terrain`. Water is never static.
- **Every 150 m-map distance must scale with `TerrainGrid.SizeScale`** (Small/Medium/Large = 111/151/191 ISLAND verts). A spawner or placement rule with a literal distance is wrong on two of three sizes. **The map is the island plus `TerrainGrid.OceanMargin` 20 m of open sea on every side (2026-09-09):** `VertsPerSide` = island verts + 40, `Half` is the map, `SizeScale` and the generator's `scale` use `Half − OceanMargin`. The cove shelf used to sit 5 m from the edge, where the depth map and fog mask stop hard.
- Seed flow: `TerrainGrid.RunSeed` set in Awake; `MenuFlow.NewGame` clears it, `Restart` keeps it; under the sim the inspector/sweep seed always wins.
- `IslandSettings.asset` is the tuning surface, rewritten from `CreateDefault()` only when the code's `CurrentVersion` is newer. `ScatterSettings.asset` is the reverse — `Build Scatter Settings` REWRITES it from `LowPolyScatter.Table` every run. Styles are applied by `IslandSettings.WithStyle` → read `TerrainGrid.ActiveSettings`, never the asset.
- **A cliff has to land inside ONE 1 m cell** (`cliffSharpness` 0.04; flood fill `maxWalkableStep` 0.9) — the agent climbs 0.75 m steps, so a two-cell step is climbable. Revisit both if spacing or agent climb changes.
- **`IsReachable` is not "a worker can work here" (2026-09-08).** The flood fill joins cells across 0.9 m steps; the bake stops at 45° and erodes by the agent radius, so a cliff-face vertex reads reachable with no walkable ground near it. Every gameplay object a unit must STAND at goes through `ResourceNode.HasStandingRoom` (8 ring samples, height-matched, reachable, 2 sides is enough) — `PropScatter`'s gatherable rules and `ResourceSpawner.IsTerrainOk` both do. `GetMaxWorkers` answers 0 when no side is open, so a boxed-in node stops advertising room.
- **Buildable = dry ∧ gentle ∧ REACHABLE** (`IsReachable` from the validator's flood fill). Ramp repair, seed reroll (`seed + attempt × 7919`), best-attempt fallback. Anchors (axis swing, land wedge cove→campfire, fixed-height cove shelf/ramp discs) make random seeds safe for the opening. Ponds only below 3 m.
- Bands are classified from the smooth field gradient at the centroid, not triangle normals (checkerboard otherwise). Adding a band = `Surface` enum + `Classify` line + `TerrainSetup.SurfaceMaterialKeys` + `LowPolyPalette` entry.
- Scatter rules are terrain-based, never radial; own `_Scatter` root; own `System.Random`; decor static-batched. **Gatherable / salvage rules build gameplay objects:** empty root with `ResourceNode` / `GroundPickup`, art on a child named exactly **"Model"** (load-bearing), under `_Scatter_Nodes`, never batched, rejected on unreachable ground, NavMesh-snapped. A depleted scattered palm respawns a broadleaf (accepted).
- `LowPolyScatter` grounds props by sampling the heightfield (chunk colliders only exist in Play mode). `TerrainSetup` snaps props, not their group roots.
- `FlattenArea(center, radius, blend)` rebuilds touched chunks, kicks `UpdateNavMeshAsync`, and re-bakes the water depth map. Ghost previews at the center height = where the pad ends up.
- **Water depth comes from the heightfield, not the camera depth texture** (`_HeightMap` / `_MapParams` on the renderer's property block; clamped; `BuildDepthMap` forces a 2-texel border ring to open-ocean depth (2026-09-07) because the cove shelf disc can reach the last column on some seeds and Clamp then paints that shallow texel to the horizon — never Repeat). Ortho has no specular spot: sheen is a soft `pow()`, sparkle is per-facet (`FacetValue`, quad count forced even so vertices land on `_GridStep` multiples), wavelengths ≥ 6× grid step. Tuned values live in `Mat_Water.mat`, not shader defaults. Shaders are not Roslyn-checked — a broken one renders magenta.
- `BaseBuilding.GetValidSpawnPosition` has no `+1` after `SamplePosition` (flat-world relic).

---

## Items, Crafting, Research, Player Gotchas

- Two `Collect` methods on `GroundPickup`, don't unify: workers get `amount` of `resourceType` into the carry (plus the material on `bb.carryItem`, banked to the stockpile on delivery); the character gets `itemId × itemAmount` or the resource in hand. `allowOverfill` on salvage (a 6-food crate must not evaporate into a 5-carry). Only what `PickupSpawner` placed is `spawnerOwned` (respawn budget) — salvage and shed byproducts are not.
- `ItemKind` says which layer an item lives in: resources exist only in hand (deposit → pool), materials go to the campfire `Stockpile`, tools stay in the player's hands, equipment is consumed per warrior. `ItemCatalog.Stockpiled` = materials only. One colony store: the Workshop resolves to `BaseBuilding.FindAlive().Stockpile`.
- Stockpile capacity is a delegate (`Inventory.totalCapacity`): base 60 + `Knowledge.StockpileRoom`; `Add` / `SpaceFor` clamp, overflow is lost.
- **A queue nobody stands at does not move** (`CraftStation.AddLabor`; `IsWorked` = labor in the last 0.5s). **A bench holds up to `MaxLaborers` 4 pairs of hands, one LANE each = one repeat of one entry (2026-09-13):** lanes on an entry never exceed its `remaining`, so research is one-at-a-time and five spears finish together; seats for colonist claims = `min(4, TotalRepeats)`; the player evicts the stalest colonist lane when full and takes over a repeat when none is free; an abandoned lane banks its progress on `QueueEntry.progress` and the next hands inherit it; `entry.live` is display only. Read a laborer's own repeat via `EntryOf(who)` / `Progress01Of(who)`, never `Active`. The player's `TaskKind.Work` walks to the approach point; any other command leaves via `StopWork`. **Costs are paid on completion; a short entry WAITS at 100%** ("Waiting for 2 Stick"), never fails. Research is de-duplicated across stations.
- **Jobless = utility labor; a specialist is jobless but NOT idle (2026-09-07).** `Worker.specialty` (`Any` / `Builder` / `Crafter` / `Repairer`) replaces the Crafter job; `Worker.IsIdle` (`!hasJob && Any && !leaving`) is the ONLY idle test (idle count, `FindIdleColonist`, recruits). `SpecialtyAllows(trade)` is a zero-cost gate beside `IsJobless` on Build / Craft / Repair, `SpecialtyAllows(Any)` on Forage (specialists never tidy); the research gates stay inside the scans and `BaseBuilding.UnlockFor` mirrors them for `AssignSpecialist`. The order is `Faction.Priorities` (a `LaborPriorities` instance, 1.0 / 0.95 / 0.9 / 0.85, four sliders on the Colonists tab, read live by the `LaborPriority` consideration from `bb.faction`; base priorities are 1.0 and a weight of 0 is the off switch) and it only breaks distance ties (every scan floors at 0.15). Left-click on a jobless colonist opens the panel there. The bench has two "mine"s: `CraftStation.Claim/Release` is who WALKS there (any jobless colonist; two spread over two benches), `AddLabor`'s laborer is who WORKS it — the player never claims and always preempts (`AddLabor` refuses a busy bench to everyone but a `PlayerCharacter`; the colonist waits beside it). Workshop speeds `{2, 2, 1, 1}`: making is fast, research is 1× everywhere.
- Stations and `RaidDirector` are **runtime-added in `Awake`**, so their `public` fields are the LIVE values — never add them to a scene or prefab by hand (the inspector copy would silently win).
- **Every gate is one `Knowledge.Has(...)` at the site that already decides the action** (`AssignWorker`, `CanRecruitWarrior`, `StartPlacement`, `SelectBuilding` for the Workshop, first line of `ConstructionAvailable` / `RepairAvailable`). Locked UI names its research via `Unlocks.ResearchTitleFor` → `ResearchCatalog.TitleGranting`. Nothing is unlocked under the sim — policies research like a player.
- **A research hands over its tool** (`ResearchDef.tool`, delivered by `CraftStation.TryComplete` to whoever stood at the bench); there are no tool recipes. A new tool = a `tool =` line on a research entry.
- **A warrior costs a weapon + 15 food + an idle colonist.** `SpawnWarrior` takes `BaseBuilding.SelectedWeapon` (the Arm-with picker; defaults to the best in stock) and sets `Warrior.weapon` right after `Instantiate`, before `Start` copies its `EquipmentDef` into damage / range / cooldown (before `SimOverrides.Apply`). `ItemCatalog.WoodenSpear` = 25 / 2 / 1.2, the live prefab numbers — retune the spear, not the prefab.
- **`ItemCatalog.Weapons` is a RANKING, best first** — the picker's default, `FirstWeaponInStock`, the sim and `BetterWeaponInStock` (never crosses melee↔ranged) all read it; a new weapon goes in at its rank. **`Warrior.ApplyWeapon` is the only place weapon stats land** (Start and `BaseBuilding.RearmWarrior`); it refreshes the blackboard and stopping distance. Rearm is peacetime-only, priority 0.5, zero momentum.
- **An archer is a warrior whose weapon says `ranged` (2026-09-04)** — no subclass, no second action: `bb.isRanged` flips `EngageEnemyExecutor.AttemptAttack` to `CombatEffects.FireArrow` (pool of 32 `Projectile`s, one shared mesh/material, damage decided at loose time, flies headless, no LOS). The range hold IS the existing edge-distance range check at reach 9; no kiting. `Warrior.ShowBody` toggles the plumber's `Model` / `Model_Archer` children — a prefab without the alt body just keeps the spearman.
- `PlayerCharacter.ToolFor` gates hand-harvest: tree → Stone Axe, rock → Stone Pick, ore → Metal Pick, food free. A new node type needs a line or it harvests bare-handed. Reach is measured to the node CENTRE against `GatherRingRadius`. `GatherResources(..., shedByproducts)` sheds sticks/chunks for workers (every 4 units, `maxLooseByproducts` 2) and gives them in hand to the player; a full inventory calls `ResourceNode.ShedOneByproduct()` instead (the pooled resource never overflows to the ground — a stick is worth 3 wood).
- Deposit has two reaches: a ground click within `DepositClickRadius` 3.5 of the fire's collider EDGE is a `DepositNear` — walk to the CLICKED point, `DepositAll` on passing within `DepositEdgeDistance` 2.4 or on arrival, a plain walk with nothing depositable (2026-09-13); a click on the fire itself walks to the approach point, deposits and works the queue. `PlayerCharacter.Stalled()` (0.5s of no path and no velocity) counts as "as close as the NavMesh allows": within 2.5u the interaction happens, beyond it the task drops.
- **The castaway has an order queue (2026-09-13):** `HandleCommandClick` → `Resolve` (an `Order`) → `Issue(order, queue)`; `KeyBindings.Action.QueueCommand` (Shift) appends while `Busy`, else the queue is cleared and the order runs. **`Continue()` is called ONLY where an order ENDS** (collect taken, deposit done, node empty / hands full, queue dry, site finished, walk `Arrived()`, every "Can't reach"); the `Stop*` methods are what a NEW command calls first, so they never continue. Every public `Command*` / `WorkAt` / `CommandMove` calls `NewOrder()`, which clears the queue unless `dispatching`. `PlayerClickMarker` (runtime-added, headless no-op) flashes a ring per `Issue` and draws the agent's path via `GetCornersNonAlloc`, **each segment sampled every 0.75 m and draped** (a straight corner-to-corner run cuts under a rise). `GhostPlacer.ConfirmPlacement` keeps the ghost while the same key is held (Shift + left-click places the next hut).
- `HeldItem` is visual only; nothing reads the held tool. `PlayerProfile.Name` never returns empty; the popup is skipped under the sim and on Restart, and the sim never writes PlayerPrefs. `FloatingText.alwaysShow` bypasses the state-label setting for the name label. `CameraController.CenterOn` intersects at the target's own height.
- **A `public float` on a unit script is DEAD DATA — the prefab value wins;** values assigned in `Start()` make the prefab the dead data. Change both and keep the comment honest. `ConstructionSite.LaborFactor` is a const for this reason.
- Large pickups scale the ROOT (the click collider is added to the root in Awake). Counts and sizes are `PickupSpawner` scene values.

---

## Opening Sequence & Startup Contract

- `GameStartController` phase machine `Landing → PlacingCampfire → Settling → Colony`; `Phase` returns `Colony` when no controller exists. During the intro `BuildPlacement` is disabled, `DayNightCycle.clockPaused` holds the clock, and the campfire is absent. **Anything that assumed a campfire at frame 0 must subscribe to `GameStartController.OnColonyStarted` or poll `BaseBuilding.ActiveList`.** The campfire is deliberately not a `BuildingType`.
- `Campfire.prefab` is the single source of truth (the setup tool bakes old scene overrides in and deletes the scene instance).
- Startup order: `TerrainGrid` (-100) → `SimRunner` (-1000, sim only) → `PauseController` (-50) → everything else.
- Debug flow (F4): yield a frame after spawning the campfire AND after spawning huts before assigning workers (housing registers in Start). Skip-to-day increments `currentDay` only when crossing midnight. Runtime huts need `layer = "Buildings"`.
- **A run starts at 8 am: scene `currentTimeOfDay` 0.33333 (2026-09-13)** — 0.25 is the MIDDLE of the `transitionWidth` 0.1 dawn blend, which read as night. Day 1 is ~17 s shorter than the rest; nothing on day 1 waits for the 0.25 crossing.
- `RaidDirector.RollForTonight` runs from `OnDayStart` plus once in `Start` for day 1. `GameManager.daysToSurvive` has no `FormerlySerializedAs` from `nightsToSurvive` on purpose. `GetDaysSurvived` = calendar day − 1 in both endings.

---

## Balance Simulation Harness (`docs/SIMULATION.md`)

- **Two sim flags, and only one may decide anything (2026-09-10):** `SimHooks.Simulating` is POLICY (difficulty snapshot, name popup, end screen, dev quests, PlayerPrefs, mouse UI, terrain seed); `SimHooks.Headless` is CAPABILITY, read ONLY to skip drawing (VFX, health bars, state text, fog upload, `OccluderCutout`, `UnitHoleMask`, decor, HUD, minimap, clouds). `SimHooks.Visual` = the pair. That split is what lets `-Visual` render a run that decides exactly what the headless sweep did. A cosmetic system must never draw from `UnityEngine.Random` (the harness seeds it and AI stagger draws from it) - use `CosmeticRng`; and a headless skip must never shorten a shared random stream (`PropScatter` skips decor at the `Instantiate`, never at the rule).
- **Visual sim = `run-sim.ps1 -Visual`** (drops `-batchmode -nographics`, adds `-simvisual`; implied in the editor). Speed comes from `OnDemandRendering.renderFrameInterval`, NEVER `timeScale`. `SimSpectatorCamera` re-scores six shots (Campfire > Landing > Battle > Raiders > Castaway > Colony) twice a second with a 4 s hold — **Raiders follows the raider nearest the fire and dies once it stops moving for 6 s, Battle needs both sides in the cluster** (2026-09-10: the centroid of a stuck straggler and the wall fight parked the view on empty ground until the dawn-hold cap), Castaway is the character on an errand by day — driving `CenterOn`/`SetZoom` while `CameraController.SuppressInput` leaves the clip-plane fit running. Unity sets window SIZE from the command line but not POSITION - `run-sim.ps1` tiles via `user32!MoveWindow`.
- **The lab is `run-sim.ps1 -Lab`** (2026-09-10): seeds are ROWS, strategies are COLUMNS, emitted row-major because the tiler fills left-to-right, and `terrainSeed` tracks the seed so a row is ONE island played three ways. Implies `-Visual`, one process per cell; **the default grid is 3 seeds × 3 strategies and the cell size is the working area divided by the grid**, so nine windows cover the screen (2026-09-10). **The draw rate follows the raid (2026-09-10): `renderFrameInterval` 11 (~4×) while the island is quiet, `renderFrameIntervalRaid` 5 (~2×) while any `Enemy` is alive**, switched by `SimRunner` once a second (`-RenderInterval` / `-RaidRenderInterval`); the draw interval is the ONE speed knob that leaves decisions alone, so it is the only one that may move. Going much lower with nine windows on one GPU risks a window drifting behind the others in GAME time. Spectator zoom is `SimSpectatorCamera.ShotZoom` (13/8/12/9/12/8, floor `CameraController.minOrthoSize` 5). The caption prints the player governor's `GovernorPolicy.Goal` / `Intent` (set by the shared moves, read by nothing that decides) and the castaway's errand. **A lab DEFEAT respawns the same cell (`_tryN`) while the process is under `respawnWallMinutes` (`-RespawnMinutes` 10); the lab's `maxWallSecondsPerRun` is 3600** because a watched 30-day run is ~20 min. The console dashboard cannot read either CSV - both are appended only when a run ENDS - so every run overwrites `status.csv` in its shard dir once a REAL second (`SimStatus`, throttled in `PushOverlay` because headless is 15–30 game seconds per real second; both modes since 2026-09-11, so `run-sim.ps1` draws the same dashboard headless with a finished-runs counter); the survive tally comes from the shards' `runs.csv`.
- **NEVER speed a sim up with `Time.timeScale`** (frame-based AI budget and NavMesh throttles would report AI starvation as balance). `SimRunner` sets `Time.captureDeltaTime = 1/60`; measure with `Time.time`.
- Reset `Time.timeScale = 1f` at the start of every run (game over sets 0 and it survives scene loads); `maxWallSecondsPerRun` failsafe. `AddComponent<T>()` runs `Awake` immediately — create the GameObject inactive, populate, activate.
- Unit knobs: `SimOverrides.Apply(this)` at the top of each unit's `Start`. `terrainSeed` inside `TerrainGrid.Awake`. Other scene knobs from `sceneLoaded`, except run 0 (set in `Bootstrap`) and anything consumed in an Awake (`ResourceManager` — write the live pool too). `-1` is the don't-override sentinel.
- `FactionBuilder` (`SimBuilder` until 2026-09-16, one instance per colony) mirrors `GhostPlacer.ConfirmPlacement` / `WallLinePlacer.ConfirmWallLine` step for step — change both together (slice B2 ends this). **A wall ring's openings must end as gates, never holes (2026-09-10):** `PlaceWallRing` leaves a two-cell opening per side and `GateOpenings` walls each cell and converts it the tick it finishes (two gates side by side, so a column paths through). The old `ConvertGates` gated the two NEWEST walls wherever they stood; twelve lab runs never saw a wall hit because raiders walked the holes. A gate carves nothing and its trigger forces a raider onto it, so a gated ring is where the fight happens. **An unbuildable ring cell is notched one cell inward (three-cell U) and anything else is a counted hole (`FactionBuilder.RingHoles` → `ring_holes`, 2026-09-10):** the third lab's Turtle lost every holed ring and won the closed one. `FactionBuilder.SetRing(half)` (Turtle/Eco, half 12) keeps huts, the Workshop and the Shipyard off the ring line (±2 cells) and out of the gate corridors (5 cells deep, both sides) — huts on the 7–16 m spiral sat in the openings of the old 9-cell ring and sealed it. `days.csv` also logs `idle_dawn, weapons_dawn, sticks_dawn, chunks_dawn, queue_dawn, warriors_lost` so a wipe that never re-arms reads as a spear-kit famine, not a raid too big. `SimTools` passes `MainIsland` to `BuildPipeline` explicitly.
- **The governor counts the rack as soldiers (2026-09-16, the levy in the sim):** `ColonyState.SpareWeapons` / `Mustered` (off `faction.Militia`, 2 Hz) give `Levy` (mustered + spare weapons capped by bodies) and `Strength` = warriors + levy; `KeepArmy(s, wanted, fullTime)` keeps the RACK at `wanted − warriors` (bodies-capped, the mustered not re-queued) and recruits a full-timer only under `fullTime` = `FullTimeWanted(wanted, floor)` = `wanted × (1 − LevyShare)` (base 0.5, Rush 1/3, Conqueror 0 — a landing needs real warriors); `WantedColonists` sizes the beds so the levy has bodies; a landing's surplus is over the FULL-TIME want. Telemetry: `days.csv` ends `spare_dusk, mustered_peak, levy_lost, asleep_landing`, `runs.csv` ends `peak_mustered, levy_lost`, `status.csv` ends `levy_mustered, levy_spare` (dashboard `war` "5+3" + `rack`), the report gains `levy` / `l.lost`; **`warriors_lost` is now the PLAYER's full-time warriors only** (`Warrior.OnAnyWarriorKilled(Warrior)` is the typed event; the old one counted a rival's militia). Sim rows before this are not comparable.
- **`KeepSpears` counts spears and bows APART (2026-09-16):** bows at most a third of the wanted stock, spears kept on their own count — one pool let bows nobody would pick fill the quota and every recruit took the only weapon left, so Turtle fielded 59% archers (12 dmg) and lost 30-42% of its militia a night to Rush's 13%. Turtle's list now has `mining` + `iron_work` before `bowyery` for the same reason. **A ring cell raiders cannot cross (unreachable or no NavMesh in the cell, `FactionBuilder.RaidersCanCross`) is sealed by the island: no wall, no detour, not a hole** — terraced seeds read 15-27 permanent "holes" that were cliff and sea; `MaxDetourDepth` is 6. What is left after that (777: 24, 101: 12) is walkable steep ground the notch cannot close — still open. The bow share is a third of the GARRISON (`ArcherCount()`), never of KeepArmy's two-weapon buffer (a third of 2 is 0 bows). **The sim's game-time cap follows the calendar (`SimRunner.CalendarCapSeconds`: (days + 1) × (cycle + dawn hold) + 300, `maxGameSeconds` only a floor):** the flat 6000 s timed out eight legitimate 30-day runs on held-dawn islands. **Every policy sizes its militia against `ColonyState.NextRaidSize`** (2026-09-10: tonight's `PlannedSize`, else `RaidDirector.EstimateRaidSize(day + 1)`, which never touches `LastProsperity`) — Turtle 0.6×, Eco 0.7×, Rush 1.0× — and buys beds ahead of the arrivals it needs (`KeepHousing`: roster + free beds vs workers + warriors + reserve). **Workers scale with the army** (`GovernorPolicy.WantedWorkers(s, floor)` = `max(floor, warriors / 2)`, capped at the campfire's `maxWorkers` 10): the second lab lost Rush to food, five workers feeding 25 warriors. Spearcraft is third on every list, no policy builds a Watchtower, and `HireWorker` has a fourth METAL share (nobody mined in the first lab because nobody asked, so Iron Work never came). The first lab (2026-09-10, `SimLogs/lab-2026-09-10/`) lost 9/9 without this.
- `SimPlayerDriver.Tick` (polled before every policy tick) drives the player character as bench labor, and as a builder on the nearest site whenever the bench has no work (2026-09-10). **`GovernorPolicy.BuilderReserve` holds 1 colonist out of every job from day one (2 past six colonists), and `Recruit` respects it except on a raid night** — the old "reserve only while a site exists" guard came too late: the policies employed all three starting colonists before Construction was researched, and the colony was then deadlocked forever.
- **The harness knows the neighbour (2026-09-16), and every rival column is about the FIRST rival to land:** `runs.csv` ends `rival_strategy, rival_fate, rival_fell_day` (`none` / `alive` / `fell` / `deserted`; only raiders can fell a fire, and a rival's loss ends nothing, so `SimRunner.WatchRivalFire` records the day per frame); `days.csv` ends `rival_fire_pct` (−1 until one lands) `rival_huts, rival_colonists, rival_food_dawn`; `status.csv` / the dashboard `rival` column / the caption carry personality + `Diplomacy.Word` + militia + fire + a `*` for a party at sea; `SimSpectatorCamera.Shot.Rival` (score 40, between Raiders and Battle) frames a party at sea, else the rival fire on its landing, a departure or a raid rolled onto its shore. `run-overnight.ps1` has a fifth default sweep `rivals` (`r0` control / `r1` / `r1turtle` `r1rush` `r1eco` / `r2` × 6 islands, 108 runs) and `summarize-sim.ps1` prints a *Neighbours* table only when a rival landed; `SimSweeps/rivals.json` is the quick version. New CSV columns go at the END — every reader is positional or by name.
- **Not run-to-run deterministic** (async NavMesh rebuilds, job-system order, `-Parallel` contention): a seed makes runs comparable, not reproducible. n=12 win-rate CI ≈ ±13pp; read `campfire_hp_min` in `days.csv` first. Never report a one-or-two-run win-rate difference as a finding.
- Sweeps are NOT comparable across: gatherable palms (09-02), salvage, colonist pool, player character (starts with 0 colonists), research/craft split (0 research, 0 spears), hauled materials, foraging (09-03), the Crafter job and food consumption (09-04), the decor/RNG parity fixes that moved headless node layouts (09-10), sleep + the 150/75 clock + the Persona draw on the shared stream (09-16 evening). Re-run baselines after any of these.
- **The sim player is built with Unity audio disabled** (2026-09-10): `SimTools` flips `ProjectSettings/AudioManager.asset`'s `m_DisableAudio` for the build and restores it in a `finally`. `AudioListener.volume = 0` is too late — the device is already open, and nine lab windows each opening one took the machine's audio driver down.
- A script-only player build rewrites `<player>_Data/Managed/Assembly-CSharp.dll`, not the .exe timestamp. Never let two sim processes share one `outputDir` (`run-sim.ps1 -Parallel N` shards). ~23–25× realtime per process.
- **Overnight = `run-overnight.ps1` (2026-09-11):** keep-awake, batchmode rebuild (**editor closed** — it tests whether `Temp/UnityLockfile` is HELD, a stale file passes), DLL-freshness check (**DLL at least as new as every `.cs` under `Assets` + "Build Finished, Result: Success" in `build.log`** — Bee copies the DLL with its COMPILE time, so "newer than the build started" failed a good build, 2026-09-11), then `baseline` / `raids` / `difficulty` / `islands` headless at `-Parallel 8` into `SimLogs/overnight-<date>/<sweep>/`, and `summarize-sim.ps1` → `REPORT.md` (Wilson CIs; read err/timeout and a short run count first), then SLEEPS the machine after a 60 s countdown (`-NoSleep`; a build failure never sleeps it). Ids are `<variant>__<strategy>__<island>`, `terrainSeed` = `seed`, and `<sweep>.manifest.csv` maps id → cell so nothing parses an id. **A sweep names its rule set (2026-09-11): `difficulty`, `islandSize`, `islandStyle`** are strings on `SimConfig`, published through `SimHooks.Difficulty` / `IslandSize` / `IslandStyle` by `SimRunner.Activate` BEFORE the scene loads (`ResourceManager.Awake` and `TerrainGrid.Awake` read them; run 0 from `Bootstrap`), and `Difficulty.Active` / `IslandOptions.Active` resolve them under `Simulating` — empty = Normal / Medium / Terraced. The preset never sets the calendar: a Peaceful row writes its own `daysToSurvive` 20. `Difficulty.Active` caches the parse (`LevelNames` allocates, the food tick reads it per frame).
- Sim-guards to keep: `Difficulty.Active` → the sweep's named preset, else Normal; name popup skipped; `PropScatter` runs every rule and skips only the decor `Instantiate`; `UnitHoleMask` skipped and `OccluderCutout` leaves materials stock; `GameManager.ShowEndScreen` guarded; `daysToSurvive` guarded behind `!SimHooks.Simulating` in `GameManager.Start`.

---

## Engine / Tooling Gotchas

- **Compile-verify without opening Unity: `py tools/verify-scripts.py -w`** (2026-09-09) — Roslyn in all four configs (editor, dev player, release player, Editor assembly), currently 0 errors / 0 warnings. It references the editor's `UnityEngine/*.dll` + `Unity.Scripting.dll` (`PreserveAttribute`) + `Library/ScriptAssemblies` package DLLs; never `UnityEditor.dll` beside `UnityEditor.*Module.dll` (spurious CS0433). `Scene.handle` is a `SceneHandle` struct in 6000.5 (`GetRawData()`), not an int.
- Edit `manifest.json` BEFORE launching the editor. Batchmode cannot run while another editor has the project open (`Temp/UnityLockfile`). Read the project-relative `Logs/Editor.log`.
- The upgrade flips `VersionControlSettings.asset` to Unity Version Control — set it back to **Visible Meta Files** if it reappears in a diff.
- **The build scene list is written by `MenuSceneSetup.AddScenesToBuildSettings`** (part of Setup Everything): MainMenu, then MainIsland. Never edit it by hand. **The version is `ProjectSettings.bundleVersion` only** (2026-09-08, `0.2.0-alpha.1`): the main menu and every playtest report read `Application.version`; bump it per build handed out and never type a version literal anywhere.
- **A script file's name must match its class name CASE-SENSITIVELY, and git will not tell you (2026-09-10):** `core.ignorecase` is true, so a file rewritten as `WatchTower.cs` for class `Watchtower` showed no diff, yet Unity's name check failed and the prefab's component became a missing script in the editor AND the build (the tower never had health, sight or its aura; the sim's `TowerCount` stayed 0 and every policy placed one per tick). Rename with a two-step `mv`. `Watchtower.AuraDisabled` is a const: the tower is vision-only until the archer-tower upgrade path.
- `.gitignore` covers `*.sln` and `*.slnx`. Assets re-serialize lazily on save — expect scene/prefab diffs the first time each is edited after an upgrade. New C# files get `.meta` on first editor focus.

---

## Logging Conventions

Console kept quiet on purpose (212 → 65 calls). **Before adding any `Debug.Log`, check the keep-list; if it doesn't fit, don't add it.**

- **Keep:** all `Debug.LogError`; `LogWarning` for recoverable misconfiguration (missing prefab/ref, duplicate singleton, no UI assigned, spawn fallback); `Debug.Log` only for once-per-night / once-per-game lifecycle: day/night transitions, raid summary, GameManager init / progress / VICTORY / DEFEAT, CAMPFIRE DESTROYED, ResourceManager init, TerrainGrid load line.
- **Never:** per-unit spawn/death, per-damage/heal, per-resource tick, per-button click, audio chatter, per-placement, helper init banners, Population add/remove, agent-configured dumps.
- Rule of thumb: would it spam a normal 5-minute session? Use the F3 overlay for live state instead.

---

## How to Run

1. Open `islandrts/` in Unity Hub (6000.5.9f1). **Run `Tools > Island RTS > Setup Everything (In Order)`** after pulling anything that touched art, prefabs or scene wiring — idempotent, and its order is load-bearing (Generate All Assets → Plumb Everything → Setup Opening Scene → Build Scatter Settings → Setup Terrain Scene → Setup Pickups + Workshop → Remove Legacy Victory-Defeat Panels → Setup Main Menu Scene). It leaves `MainMenu` open; `Open Game Scene (MainIsland)` jumps to gameplay.
2. Press Play. Name your castaway, right-click to walk ashore, **B** to place the campfire (free, one-time).
3. Hand-collect sticks and stone chunks, deposit at the fire (right-click), research **Woodcutting** on the campfire panel's Research tab (left-click the fire; the character must stand at the bench). Research opens jobs, build mode and Spearcraft; craft Wooden Spears to arm warriors.
4. Colonists come ashore on their own as housing allows; assign jobs on the Colonists tab; press B to build.

## Controls

WASD/Arrows · Q/E · wheel · middle-drag = pan · rotate · zoom · tilt. **B** build mode (opening: place the campfire); **1–6** Hut · Wood Wall · Stone Wall · Watchtower · Workshop · Shipyard (beach only); **G / R / Shift** wall→gate · L-path toggle or rotate · Bresenham; **Delete / X** demolish (50% refund). **F2 / F3 / F4 / F6** grid · AI overlay · debug menu · perf logger. Left-click campfire / Workshop = panel (Colonists · Stockpile · Craft · Research · Queue); Esc → INFORMATION = field guide (`Resources/Information.txt` + live tables). Right-click = character smart command (fetch · hand-harvest · deposit + work the queue · work a bench · build a site · walk); **Space** centres on the character; left-click / drag the minimap centres the camera there. **F5 / F8 / F9** militia stance Defensive / Offensive / Follow (also the bottom-right combat box with the formation buttons). All rebindable in *Options → Controls*; Esc, mouse buttons and F3/F4/F6/F7 are reserved. **Full controls: `docs/CONTROLS.md`** — keep in sync when a binding changes.

---

## Current State (2026-09-11)

Branch `feature/balance-sim-and-menus`. **Lap step 1 (factions) is LANDED, 2026-09-09: seven commits `d270593` → `eed09fb` (see PHASE_HISTORY), all green in the four Roslyn configs; the commit-5 regression sweep passed and exposed the contaminated baseline (only its `*_s1` rows count — `SimSweeps/baseline-2026-09-09/README.md`). Unplaytested: the "Factions step 1" batch in `DevQuests.txt` is the checklist, F4 → Rival camp is the demo. Next: step 2 (spatial hash + AI LOD) per `docs/ARCHITECTURE_LAP_PLAN.md`; a sweep after it needs the sim player rebuilt in the editor first (script-only build; check `Assembly-CSharp.dll`'s date, not the exe's). The harness stall (research done, wood plentiful, no hut placed, no spear made) is owed to the step-3 governor rewrite.** Section I (fog + minimap) is built end to end and unplaytested; its playtest and the "After fog" pass over the five reopened batches still stand. **Third lab (2026-09-10, `SimLogs/`) read and answered in four commits `2f0f87f` → `dca2ea5`: gate repair, the two-night breather, raiders never through a wall, ring notches + rebuild columns. Next lab needs the sim player rebuilt first; read `ring_holes`, `weapons_dawn` and `warriors_lost` before anything else.**

**The overnight batch (2026-09-11, `SimLogs/overnight-2026-09-11/REPORT.md`, 450 runs) is read and answered in two commits `8a2779d` + `f362ce5`.** It lost all 36 of Turtle's baseline runs and read as a wall problem; it was four unrelated bugs. Ring cells now detour inward up to 4 and holes are re-examined every 45 s; `RushPolicy` was missing `quarrying` (and `Research` skips an id whose prerequisite is unmet, so its last three entries were dead all run - it never saw a chunk); Turtle now fields 1.0 warriors per raider, not 0.6; `Prosperity` caps the hoard term, halves walls to 0.15, and **counts the PLAYER's buildings via `TargetingUtil.CountOwned` rather than the global registries** (a rival's huts were enlarging the player's raid). Nodes shed every 3 units, cap 3 loose, values written into the four node prefabs. `SimPlayerDriver` explores when the bench needs something nobody can find; `days.csv` gains `chunks_loose`. The verifying lab: Rush took victory day 30 on seeds 9001 and 101 and Eco escaped on both, islands that went 0/6 overnight. **Turtle's 1.0x is UNTESTED and the sim player needs rebuilding before the next lab.** Seed 7 stalls all three strategies at 2 workers / 0 huts - the known harness stall, owed to the governor rewrite.

**Lap step 3 SLICE A is committed `f9a00de` (2026-09-13); SLICE B1 + B3 + B5-minus-trade are built (2026-09-16), uncommitted, unplaytested and unswept.** A scheduled shipwreck lands a rival on its own far shore at `max(3, round(daysToSurvive / 3))`, territory is a per-colony gathering preference that the PLAYER pays too, and the rival now RUNS ITSELF through the sim's own policies (`Scripts/Factions/Governor/`, rules in the Rival colonies section). Green in all four Roslyn configs; the harness learned the neighbour the same day (rival columns, fate, dashboard, Rival shot, `rivals` sweep — Balance Simulation Harness section). **The overnight batch of 2026-09-16 (`SimLogs/overnight-2026-09-16/REPORT.md`, 558 runs, 8 timeouts) is read and answered (five code changes, see Balance Simulation Harness / Calendar / Rival colonies): baseline Turtle 36% / Rush 72% / Eco 86%, Brutal 0/18, large-rugged 0/12, no expedition ever sailed (no pair crossed ±40 in 108 rival runs — a design knob, untouched), and dead raid nights (raiders ashore, nobody fights, fire untouched) on seeds 8851 and 23 only — owed a visual lab on 8851 (`run-sim.ps1 -Lab` with seed 8851, watch the Raiders shot). Still owed: the quest batches above (F4 → Rival camp, F3, Hostile / Allied buttons) and a `rivalCount: 1` lab.** Then B2 (placement through `FactionBuilder`) and B4 (scored build order); `docs/RIVAL_COLONIES_PLAN.md` is the live plan. Step 2 (spatial hash + AI LOD) is deliberately skipped for now.

**Shipped:** four-resource economy with a colonist pool (jobless colonists build/craft/repair/forage with Builder/Crafter/Repairer specialists); player character with hand-harvest and campfire deposit; research → craft split with stations, per-warrior weapons (spears, Iron Spear, Bow); 30-day calendar with dawn-rolled raids; walls/gates/towers/demolish; Utility AI; random islands with stylized water and runtime scatter; occluder cutout windows (trees, buildings, walls); code-built menus, changelog and Information screens; a daily sky (clouds + cookie shade) and graphics presets; F4 debug menu; headless balance sim.

**Pending playtest:** everything in `DevQuests.txt` — 39 batches, 169 open quests after the first report (`Playtests/playtest_2026-09-13_2204.md`, 2026-09-13: the loop works, 99 closed, the Stone pile batch gone). Ship blockers A1/A2/A3/A5 are closed, A4 is one release build to play. `DevQuests` is editor/dev-build only (`const Enabled`, `#pragma 0162`); quest ids are batch-slug + index, so append rather than insert, and **a prune that removes middle quests needs one RESET on the DEV tab** (owed after the 2026-09-13 prune). A raid tuning pass is pending.

**Next: THE ARCHITECTURE LAP (decided 2026-09-09, freeze lifted): Factions → spatial hash + AI LOD → colony governor (+ diplomacy, dock trade) → save/load (dawn checkpoint) → archipelago islands.** `docs/ARCHITECTURE_LAP_PLAN.md` is the plan: per-step shape, migration commits, definitions of done, the 2026-09-09 decisions and the open ones. Rules while it runs: every step green in all four Roslyn configs + a sim regression (Turtle/Rush/Eco n=6 against `SimSweeps/baseline-2026-09-09/`, taken BEFORE step 1) before commit; a compatibility shim lives one step then dies and its accessor joins the banned list; **faction is read in `Start` or later, never `Awake`** (`Instantiate` runs Awake before the owner can be set); one data model (`ColonyLedger`) for the abstract tier, the save file and island travel. Then `docs/ALPHA_PLAN.md` B → G (playtest debt → tuning → tutorial → feedback form → zip) on the new foundation. A bug found in playtest gets fixed, a wish gets written down. Parked: the Story tab, weather part two, `docs/plans/COLONY_EXPANSION_PLAN.md`, Phase 10 Stages 3–4, the Warehouse, building upgrades, shore-wading enemies.

---

## Balancing Reference

| Unit | HP | Damage | Attack Speed | DPS |
|------|----|--------|-------------|-----|
| Warrior (Wooden Spear) | 75 | 25 | 1.2s | 20.8 |
| Warrior (Iron Spear) | 75 | 35 | 1.2s | 29.2 |
| Archer (Bow, range 9) | 75 | 12 | 1.0s | 12 |
| Enemy | 50 | 10 | 1.5s | 6.67 |

| Building | Cost | HP |
|----------|------|----|
| Hut | 20W 10F | 100 |
| Wooden Wall / Gate | 15W 5S | 150 / 75 |
| Stone Wall / Gate | 10W 20S | 300 / 150 |
| Watchtower | 25W 15S | 200 |
| Workshop | 30W 20S | 150 |
| Shipyard (beach only, 45s×2 build; Shipwright 40W 30S 10M) | 200W 120S 30M | 300 |
| Warrior | 1 Wooden Spear + 15F + an idle colonist | 75 |
| Wooden Spear (craft, 10s) | 3 stick 1 chunk 5W | — |
| Iron Spear (craft, 12s; Iron Work 20W 25S 10M) | 2 stick 5W 4M | — |
| Bow (craft, 12s; Bowyery 20W 5F) | 4 stick 5W | — |

- Starting resources 100W 50F 0S 0M (metal buys Iron Work and Iron Spears). Workshop makes tools and weapons at 2×. Worker carry 5, gather 1/sec; stick = 3 wood, chunk = 3 stone, crate = 6 food, barrel = 5 wood; large pickups ×3.
- Raid size `round(2 + 0.4 × day + 0.08 × prosperity)`: day-4 ≈ 5, day-20 ≈ 16, day-30 ≈ 22; roughly every 3 days, spawned 0.4s apart so a raid arrives as one body.
- Food: 1 per colonist per calendar day (× difficulty 0.5–1.5); Hungry after 0.25 day (labor ×0.6, no arrivals), Starving after 1 day (one colonist leaves per day). Warrior heal at campfire 1.5 HP/s (deliberately slow). One builder finishes a site in `buildTime × 2` (10s), up to 3 stack. Repair = 25% of build cost per full repair. Stockpile 60 (+40 Storage Pits, +80 Racks and Baskets).
